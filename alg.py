def calc_error(input_data,net,exp_out): # Takes training data, the network, and the expected                                            # output. Outputs a matrix (array of arrays)
    return #error_mat

def grad_desc(error_mat,net_in): # Takes the error matrix and the original network, performs
                                 # gradient descent, and returns new matrix. This encompasses
                                 # backpropagation.
    return #net_out

def train(inputs,net,num_step): # Takes input data, the network to train, and the number of                                     # times to run the training. Returns final network.
    return #final_net

#def train(inputs,net,epsilon): extra feature, in which the training function runs indefinitely # until its mean error is less than epsilon

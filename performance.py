import alg

#secondary objective, only to be implemented after everything is running
def breakup(all_input,batch_size): # Takes all input data, breaks it into smaller batches.
                                   # Returns an array of batches.
    return #batches

def run_tests(all_input): # Takes all input data, breaks it up using breakup, trains on all but                           # one batch, tests on that batch, prints out results of memorization
                          # and generalization
    #print(stuff)
    return
